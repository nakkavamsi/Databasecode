<Project Sdk="Microsoft.Build.Sql/2.2.0">
  <PropertyGroup>
    <Name>Databasecode</Name>
    <ProjectGuid>{A1B2C3D4-E5F6-7890-ABCD-EF1234567890}</ProjectGuid>
    <DSP>Microsoft.Data.Tools.Schema.Sql.Sql160DatabaseSchemaProvider</DSP>
    <ModelCollation>1033, CI</ModelCollation>
    <TargetDatabaseSet>True</TargetDatabaseSet>
    <DefaultFileStructure>BySchemaAndSchemaType</DefaultFileStructure>
  </PropertyGroup>

  <!-- Migrations are the source of truth; Schemas/ is generated before build -->
  <Target Name="SyncSchemaFromMigrations" BeforeTargets="Build">
    <Exec
      Command="python3 &quot;$(MSBuildProjectDirectory)/scripts/sync-schema-from-migrations.py&quot;"
      Condition="'$(OS)' != 'Windows_NT'" />
    <Exec
      Command="python &quot;$(MSBuildProjectDirectory)/scripts/sync-schema-from-migrations.py&quot;"
      Condition="'$(OS)' == 'Windows_NT'" />
  </Target>

  <!-- Migration folders must use semver: Migrations/MAJOR.MINOR.PATCH/*.sql -->
  <ItemGroup>
    <Build Remove="Migrations/**/*.sql" />
    <None Include="Migrations/**/*.sql" />
    <None Include="scripts/**" />
  </ItemGroup>

  <ItemGroup>
    <Folder Include="Schemas" />
    <Folder Include="Schemas/dbo" />
    <Folder Include="Schemas/dbo/Tables" />
    <Folder Include="Schemas/dbo/StoredProcedures" />
    <Folder Include="Migrations" />
    <Folder Include="scripts" />
  </ItemGroup>
</Project>
